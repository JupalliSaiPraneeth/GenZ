# Workspace Rules

## Git Workflow
- **Do NOT execute `git commit` or `git push` commands.**
- The user will handle all git commits and pushes manually.
- You may edit files, run builds, and run tests locally as needed.
